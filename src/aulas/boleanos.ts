// Booleanos

const isUserOnMajority = true;

// is, does, has

const isDisabled = false;

const userHasGroup = true;

// ----------------

if (isDisabled) {
  console.log("Usuário desabilitado");
}

// Booleans sempre como perguntas - semântico

const isLoading = true;

// Nomenclatura -> evite variaveis genericas: loading, disabled, data, fetching, list, est

// Booleanos sempre como perguntas

const isUserProfileLoading = true;

// if is user profile loading
